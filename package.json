{
  "name": "shopify-sentinel",
  "version": "0.1.0",
  "description": "Automated QA workflow for Shopify checkout and promo code monitoring",
  "main": "src/sentinel.js",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "sentinel": "node src/sentinel.js",
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@playwright/test": "^1.44.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-playwright": "^2.10.4",
    "globals": "^17.6.0",
    "prettier": "^3.2.0"
  }
}
